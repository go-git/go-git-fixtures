version: "2"
run:
  concurrency: 3
linters:
  default: all
  disable:
    - depguard
    - ireturn
    - mnd
    - varnamelen
    - unqueryvet
    - wrapcheck
    - wsl
    - wsl_v5
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling

    rules:
      - path: _test\.go
        linters:
          - exhaustruct
      - path: scanner_entries\.go
        linters:
          - dupl
          - lll

formatters:
  enable:
    - gci
    - gofmt
    - gofumpt
    - goimports
  exclusions:
    generated: lax
